<main class="pt-page">
  <div class="container-xl pt-content pt-narrow">
    <header class="pt-form-head">
      <div class="pt-eyebrow">Log a price</div>
      <h1 class="pt-h1">New observation</h1>
      <p class="pt-lede">Adding to <strong><%= @product.name %></strong>.</p>
    </header>

    <%= render "form", price_record: @price_record, product: @product %>
  </div>
</main>

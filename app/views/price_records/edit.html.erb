<main class="pt-page">
  <div class="container-xl pt-content pt-narrow">
    <header class="pt-form-head">
      <div class="pt-eyebrow">Edit observation</div>
      <h1 class="pt-h1">Update record</h1>
      <p class="pt-lede"><%= link_to @price_record.product.name, @price_record.product, class: "pt-link" %></p>
    </header>

    <%= render "form", price_record: @price_record, product: @price_record.product %>
  </div>
</main>

<main class="pt-page">
  <div class="container-xl pt-content pt-narrow">
    <header class="pt-form-head">
      <div class="pt-eyebrow">Edit product</div>
      <h1 class="pt-h1"><%= @product.name %></h1>
    </header>

    <%= render "form", product: @product %>
  </div>
</main>
